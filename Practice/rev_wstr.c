int main(int ac, char ** av)
{
    int i = 0;
    int start;
    int end;
    int flag;

    if (ac == 2)
    {
        whie (av[1][i])
            i++;
        while (i >= 0)
        {
            while (av[1][i] == '\0' || av[1][i] == ' ' || av[1][i] == 't')
            i--;
            end = i;
            while (
